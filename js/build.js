/* Replaced at deploy time with the real commit and timestamp.
   This value being "dev" in a browser means the page was NOT served by a
   deploy - almost always a stale cached copy. */
window.KH_BUILD = { sha: 'dev', at: 'local' };
